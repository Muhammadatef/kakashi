---
description: Rehearse every Kakashi manager case in one Cursor Agent turn
---

# Run the complete Kakashi rehearsal

Do not open, attach, quote, or summarize generated files under
`demos/.work/input/`. Do not run verbose or audit commands directly.

## Run

1. Run `node tests/cursor-demo.test.js`.
2. Run `node demos/demo.js all`.
3. Require the final `ALL 6 SELECTED DEMOS PASSED` line.

## Explain in chat

Create six short sections: file protection, folder compliance, database result
protection, Guardian, agent sidecar, and operations. For each section report the
commands exercised and the verified safety property. Use only terminal output;
never read raw generated inputs.

Finish with a concise government-value summary covering local processing, data
minimisation, purpose limitation, fail-closed approval, and value-free evidence.
